import sys\nfrom PyQt5.QtWidgets import QApplication, QWidget, QVBoxLayout, QLabel, QLineEdit, QPushButton, QMessageBox\n\nclass HotkeyCreator(QWidget):\n    def __init__(self):\n        super().__init__()\n        self.initUI()\n\n    def initUI(self):\n        self.setWindowTitle('Create Hotkey')\n        self.setGeometry(300, 300, 300, 200)\n\n        layout = QVBoxLayout()\n\n        self.label = QLabel('Enter a hotkey:', self)\n        layout.addWidget(self.label)\n\n        self.hotkey_input = QLineEdit(self)\n        layout.addWidget(self.hotkey_input)\n\n        self.submit_button = QPushButton('Create Hotkey', self)\n        self.submit_button.clicked.connect(self.create_hotkey)\n        layout.addWidget(self.submit_button)\n\n        self.setLayout(layout)\n\n    def create_hotkey(self):\n        hotkey = self.hotkey_input.text()\n        if hotkey:\n            QMessageBox.information(self, 'Success', f'Hotkey {hotkey} created!')\n        else:\n            QMessageBox.warning(self, 'Error', 'Please enter a valid hotkey.')\n\nif __name__ == '__main__':\n    app = QApplication(sys.argv)\n    hotkey_creator = HotkeyCreator()\n    hotkey_creator.show()\n    sys.exit(app.exec_())\n